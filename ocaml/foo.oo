interface I {}

class Counter implements I {
  cnt : int

  def add(n : int) : Unit {
    let old = this.cnt in
    this.cnt = old + n
  }
  def get(x : I) : int {
    this.cnt
  }
}

let c = new Counter in
let tmp = c.add(42) in
c.get(null)