interface I {}

class Counter implements I {
  cnt : int

  def set(n : int) : Unit {
    this.cnt = n
  }
  def get(x : I) : int {
    this.cnt
  }
}

let c = new Counter in
let tmp = c.set(42) in
c.get(null)